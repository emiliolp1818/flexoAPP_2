{
  "phases": {
    "setup": {
      "nixPkgs": ["dotnet-sdk_8", "dotnet-runtime_8"]
    },
    "install": {
      "cmds": ["dotnet restore"]
    },
    "build": {
      "cmds": ["dotnet publish --no-restore -c Release -o out"]
    }
  },
  "start": {
    "cmd": "dotnet out/FlexoAPP.API.dll"
  }
}
