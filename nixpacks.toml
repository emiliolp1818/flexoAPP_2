# Configuración de Nixpacks para Railway - BACKEND
# Este archivo le dice a Railway cómo construir y ejecutar el BACKEND
# IMPORTANTE: En Railway, configura "Root Directory" = "backend"

[phases.setup]
# Instalar .NET 8 SDK
nixPkgs = ["dotnet-sdk_8"]

[phases.install]
# Restaurar dependencias
cmds = ["dotnet restore"]

[phases.build]
# Compilar el proyecto en modo Release
cmds = ["dotnet build --configuration Release --no-restore"]

[start]
# Comando para iniciar la aplicación
# Usar dotnet run en lugar de ejecutar el binario directamente
# Railway proporciona la variable $PORT automáticamente
cmd = "dotnet run --configuration Release --no-build --urls http://0.0.0.0:$PORT"
