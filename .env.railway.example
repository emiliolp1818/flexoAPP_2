# ============================================
# RAILWAY - Variables de Entorno
# ============================================
# Copia este archivo y ajusta los valores según tu configuración

# ============================================
# BACKEND (.NET)
# ============================================

# Entorno
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://+:8080

# Base de Datos MySQL (Railway proporciona estas variables automáticamente)
# Usa referencias a las variables de Railway: ${MYSQL_HOST}, ${MYSQL_PORT}, etc.
ConnectionStrings__DefaultConnection=Server=${MYSQL_HOST};Port=${MYSQL_PORT};Database=${MYSQL_DATABASE};Uid=${MYSQL_USER};Pwd=${MYSQL_PASSWORD};

# JWT - IMPORTANTE: Cambia estos valores
JWT_SECRET_KEY=CAMBIA-ESTO-POR-UNA-CLAVE-SEGURA-DE-MINIMO-32-CARACTERES-AQUI
JWT_ISSUER=FlexoAPP
JWT_AUDIENCE=FlexoAPP-Users
JWT_EXPIRATION_MINUTES=60
JWT_REFRESH_EXPIRATION_DAYS=7

# CORS - Actualiza con la URL de tu frontend
CORS_ORIGINS=https://tu-frontend.up.railway.app

# Logging
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft.AspNetCore=Warning
Logging__LogLevel__Microsoft.EntityFrameworkCore=Warning

# Cache (opcional)
CACHE_ENABLED=true
CACHE_EXPIRATION_MINUTES=30

# SignalR
SignalR__Enabled=true

# File Upload (opcional)
MAX_FILE_SIZE_MB=10
ALLOWED_FILE_EXTENSIONS=.jpg,.jpeg,.png,.pdf,.xlsx,.csv

# ============================================
# FRONTEND (Angular)
# ============================================

# API Backend - Actualiza con la URL de tu backend
API_URL=https://tu-backend.up.railway.app/api
HUB_URL=https://tu-backend.up.railway.app/hubs

# ============================================
# MYSQL (Railway lo configura automáticamente)
# ============================================
# No necesitas configurar estas manualmente, Railway las proporciona:
# MYSQL_HOST
# MYSQL_PORT
# MYSQL_USER
# MYSQL_PASSWORD
# MYSQL_DATABASE
# MYSQL_URL

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. Genera una clave JWT segura con: openssl rand -base64 32
# 2. Actualiza CORS_ORIGINS con la URL real de tu frontend
# 3. Actualiza API_URL con la URL real de tu backend
# 4. No compartas este archivo con las claves reales
# 5. Railway permite usar ${VARIABLE} para referenciar otras variables
